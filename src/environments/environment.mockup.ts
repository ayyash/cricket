// tslint:disable-next-line:no-unused-expression
window && (window['_indebug'] = true);

export const environment = {
    production: true,
    language: 'en', // this has to stay here
    apiRoot: 'https://api.website.com'

};
